const Intro = (props) => {
  return (
    <div>
      <p>nama saya {props.nama}</p>
      <p>hobby saya {props.hobby}</p>
      <p>umur saya {props.umur}</p>
    </div>
  );
};

export default Intro;
